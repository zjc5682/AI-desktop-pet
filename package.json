{
  "name": "table-pet",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @table-pet/pet tauri dev",
    "build": "pnpm -r build",
    "build:pet": "pnpm --filter @table-pet/pet build"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "~5.6.2"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
