# src-tauri/python_backend/requirements.txt
# FastAPI 核心框架
fastapi==0.109.0

# ASGI 服务器
uvicorn[standard]==0.27.0

# CORS 支持（已包含在 fastapi 中，但显式声明）
python-multipart==0.0.6

# 可选：HTTP 客户端（用于调用外部 API）
httpx==0.26.0

# 可选：环境变量管理
python-dotenv==1.0.0
